#displaying the details of the given UI.

1. Clone the project "git clone "https://github.com/mkvenzon/display-details-app.git"
2. Choose HTML file and right click then run thru live server to execute the app. 
